{
  "subgraphs_reader": {
    "networks": {
      "5042": {
        "blockscout": { "url": "https://explorer.arc.io" },
        "use_protocols": ["contour"],
        "rpc_url": "https://rpc.mainnet.arc.io"
      }
    },
    "protocols": {
      "contour": {
        "tld_list": ["{{suffix}}"],
        "network_id": 5042,
        "subgraph_name": "{{subgraphName}}",
        "address_resolve_technique": "reverse_registry",
        "specific": {
          "type": "ens_like",
          "native_token_contract": "{{baseRegistrarAddress}}",
          "registry_contract": "{{registryAddress}}"
        }
      }
    }
  }
}
